import { Colors } from "react-native/Libraries/NewAppScreen";
import { StyleSheet } from "react-native";

const WrappingViewStyleDark = StyleSheet.create({
  backgroundColor: Colors.black,
  flex: 1,
});
const WrappingViewStyleWhite = StyleSheet.create({
  backgroundColor: Colors.white,
  flex: 1,
});

export { WrappingViewStyleDark, WrappingViewStyleWhite };
